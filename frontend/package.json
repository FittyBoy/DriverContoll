{
  "name": "driveelite-frontend",
  "version": "1.0.0",
  "description": "DriveElite — Frontend (Static HTML/CSS/JS)",
  "scripts": {
    "dev": "npx http-server . -p 5500 --cors -o",
    "start": "npx http-server . -p 5500 --cors"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  }
}
