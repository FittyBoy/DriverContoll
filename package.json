{
  "name": "driveelite",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":      "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend":  "cd backend && node server.js",
    "dev:frontend": "cd frontend && npx serve . --listen 5500 --cors"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
